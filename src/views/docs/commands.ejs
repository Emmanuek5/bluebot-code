<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include('../partials/meta.ejs') %>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <% include('../partials/links.ejs') %>
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="https://www.w3schools.com/w3css/4/w3.css" />
    <title><%=botname%> | Commands</title>
  </head>
  <style>
    body {
      font-family: 'Poppins', sans-serif !important;
      background-color: #1d1e28;
      color: #fff;
      -webkit-font-smoothing: antialiased;
      -moz-osx-font-smoothing: grayscale;
      text-rendering: optimizeLegibility;
      border-top: 6px #ad91ff solid !important;
      font-size: 16px;
      height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      flex-direction: column;
    }
    h1 {
      font-size: 2.5rem;
      font-weight: 700;
      margin-bottom: 1rem;
      position: absolute;
      top: 10px;
    }
    p {
      font-size: 1.5rem;
      font-weight: 700;
      margin-bottom: 1rem;
      position: absolute;
      top: 100px;
    }

    table {
      position: absolute;
      top: 200px;
      width: 100%;
      border-collapse: collapse;
      border-spacing: 0;
      background-color: #2c4185;
    }
    tr:hover {
      background-color: #2b0c81;
    }
    th,
    td {
      text-align: left;
      padding: 16px;
    }
    th {
      background-color: #2b0c81;
      border: 1px solid #ddd;
      color: white;
    }
  </style>
  <body>
    <h1><%=botname%> Commands</h1>
    <p>Commands:</p>

    <table class="">
      <thead>
        <tr>
          <th class="">Command</th>
          <th class="">Description</th>
          <th class="">Usage</th>
        </tr>
      </thead>

      <tbody>
        <% for (let i = 0; i < commands.length; i++) { %>
        <tr>
          <td class="w3-border"><%=commands[i].name%></td>
          <td class="w3-border"><%=commands[i].description%></td>
          <td class="w3-border"><%=commands[i].usage%></td>
        </tr>
        <% } %>
      </tbody>
    </table>
  </body>
</html>
